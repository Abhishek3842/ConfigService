server:
  port: 8085

spring:
  application:
    name: queries-service

  datasource:
    url: jdbc:postgresql://postgres-db:5432/complaint_db
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
eureka:
  client:
    service-url:
      defaultZone: http://discovery-service:8761/eureka
